DATABASE_URL=postgres://codeeval:codeeval@localhost:5432/codeeval?sslmode=disable
ADDR=:8080

# Piston: defaults to the self-hosted instance brought up by docker-compose.yml.
# PISTON_API_KEY is only needed if you point this at a gated/hosted instance.
PISTON_URL=http://localhost:2000/api/v2
PISTON_API_KEY=
